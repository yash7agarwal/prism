version: "3.9"

services:
  telegram-bot:
    build: .
    env_file: .env
    restart: unless-stopped
    volumes:
      # Persist memory across restarts
      - ./memory:/app/memory
      - ./webapp/data:/app/webapp/data
